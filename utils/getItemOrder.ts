// Utitity to get the order of a new item in the database

export const newOrder = (items: any[], item: any, sourceIndex: number, destinationIndex: number) => {
  // const itemOrder = item[key];
  // const orderedItems = items
  //   .map((item) => item[key])
  //   .sort((a, b) => a - b);
  // const itemIndex = orderedItems.findIndex(
  //   (orderedItem) => orderedItem === itemOrder
  // );
  // const nextItem = orderedItems[itemIndex + 1];
  // const previousItem = orderedItems[itemIndex - 1];
  // const order =
  //   itemIndex === 0
  //     ? nextItem / 2
  //     : itemIndex === orderedItems.length - 1
  //     ? previousItem + 65536
  //     : (nextItem + previousItem) / 2;
  // return order;
  return 0;
 
}